# Solution 1:
class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        
        nums=[]
        for i in range(n):
            m=start+2*i
            nums.append(m)
        
        x = 0
        for num in nums:
            x ^= num

        return x
