# Solution 1:
class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        n = list(str(n))

        p = 1
        s = 0
        for i in n:
            i = int(i)
            p *= i
            s += i
        return p-s

