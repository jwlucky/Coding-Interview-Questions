class Solution:
    def createTargetArray(self, nums: List[int], index: List[int]) -> List[int]:
        arr = []
        
        for i,r in zip(nums,index):
            arr.insert(r,i)
        return arr
