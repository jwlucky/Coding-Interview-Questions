# Solution 1:
class Solution:
    def areOccurrencesEqual(self, s: str) -> bool:
        c = Counter(s)
        n = c.values()
        return len(set(n)) == 1
        # return len(set(Counter(s).values())) == 1
